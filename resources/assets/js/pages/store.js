window.pagination = new Vue({
    el: '#vue-paginator-root'
});

window.products = new Vue({
    el: '#vue-products-root'
});

window.feedback = new Vue({
    el: '#vue-feedback-root'
})

window.authModal = new Vue({
    el: '#vue-auth-modal-root'
})

